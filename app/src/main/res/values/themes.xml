<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="Base.Theme.FoodStore" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/app_color_primary</item>
        <item name="android:actionMenuTextColor">@color/white</item>

        <!-- Customize BottomNavigationView -->
        <item name="bottomNavigationStyle">@style/BottomNavigationStyle</item>
    </style>

    <!-- Style for BottomNavigationView -->
    <style name="BottomNavigationStyle" parent="Widget.Material3.BottomNavigationView">
        <item name="backgroundTint">@color/app_color_primary</item>
        <item name="itemIconTint">@color/black</item>
        <item name="itemTextColor">@color/black</item>
    </style>

    <style name="Theme.FoodStore" parent="Base.Theme.FoodStore" />

</resources>
